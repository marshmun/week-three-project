var mainCalc = document.querySelectorAll('#calculator span');
var operators = ['+', '-', 'x', '÷'];
var decimalAdded = false;

for (var index = 0; index < mainCalc.length; index++) {
    mainCalc[i].onclick = function (e) {

    }
}